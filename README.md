# jd-matcher
Ranks candidate profiles against job descriptions with embedding cosine similarity, no model call per comparison, then demonstrates where that fails: semantic similarity encodes what a document is about, not whether it satisfies a hard constraint.
